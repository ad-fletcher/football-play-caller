name: football_drive
version: 0.2.0
description: "Adversarial NFL play-calling RL environment — offense vs defense over full drives"

client:
  class_name: FootballDriveClient
  module: football_env.client

action:
  class_name: GameAction
  module: football_env.models

observation:
  class_name: GameObservation
  module: football_env.models

default_image: football-env:latest
spec_version: 1
